{
    "task": "binary",
    "num_of_seeds": 1,
    "initial_seed": 10,
    "epochs_per_seed": 1,
    "train_batch_size": 1,
    "max_seq_length": 50,
    "models": [
        {
            "model_name": "ConfliBERT-scr-uncased",
            "model_path": "snowood1/ConfliBERT-scr-uncased",
            "architecture": "bert",
            "do_lower_case": true
        }
    ]
}
